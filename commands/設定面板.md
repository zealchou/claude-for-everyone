---
description: 打開「設定面板」，逐條查看與開關每個功能（用講的就能操作）
---

請幫我打開「新手友善包・設定面板」，全程白話、不要貼技術細節：

1. 先讀這幾個地方，才能正確判斷每個功能現在是「✅ 開」還是「⬜ 關」：
   - `~/.claude/settings.json` → 講人話、語音提醒、白話狀態列
   - `~/.claude/hooks/folder-guard.sh`（看裡面 PROTECTED_DIR 有沒有填）→ 資料夾安全鎖
   - `~/.claude/CLAUDE.md`（看 `<!-- newbie-kit:會自己長大 -->`、`<!-- newbie-kit:出廠檢查 -->` 這兩段在不在）→ 會自己長大、出廠檢查
   - `~/.claude/scripts/notify-telegram.sh` 與環境變數 → 傳到 Telegram（選用）

2. 用白話**表格**告訴我每一項的狀態：
   - 🗣️ 講人話　🔔 語音提醒　🔒 資料夾安全鎖　📊 白話狀態列
   - 🌱 會自己長大　🔍 出廠檢查　📤 傳到 Telegram（選用）

3. 等我用白話告訴你要**開 / 關 / 調整**哪一個，你就幫我改到「對的地方」：
   - 語音 / 狀態列 / 講人話 → 改 `settings.json` 的對應段落
   - 安全鎖 → 改 `folder-guard.sh` 的 PROTECTED_DIR（要關就清空、要開就填資料夾路徑）
   - 會自己長大 / 出廠檢查 → 改 `CLAUDE.md` 對應的 `<!-- newbie-kit:... -->` 標記區段（要關就把整段移除或註解掉）
   - Telegram → 協助我設定機器人 token

4. 注意誠實說明：「會自己長大 / 出廠檢查」是寫在 CLAUDE.md 的**長期指令**，關掉是「降低它主動做的傾向」，不是機器層級的強制關閉。
   風險動作（例如安全鎖範圍）動手前先跟我確認。改完用白話說「現在變成怎樣、要不要重開 Claude Code 才會生效」。
